{% extends 'base.html.twig' %}

{% block title %}Meslanguage{% endblock %}

{% block body %}
    <h1>Meslanguage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ meslanguage.id }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ meslanguage.niveau }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('meslanguage_index') }}">back to list</a>

    <a href="{{ path('meslanguage_edit', {'id': meslanguage.id}) }}">edit</a>

    {{ include('meslanguage/_delete_form.html.twig') }}
{% endblock %}
