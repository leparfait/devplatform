{% extends 'base.html.twig' %}

{% block title %}Diplome{% endblock %}

{% block body %}
    <h1>Diplome</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diplome.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ diplome.titre }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ diplome.date ? diplome.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ diplome.description }}</td>
            </tr>
            <tr>
                <th>Competence</th>
                <td>{{ diplome.competence }}</td>
            </tr>
            <tr>
                <th>Institut</th>
                <td>{{ diplome.institut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('diplome_index') }}">back to list</a>

    <a href="{{ path('diplome_edit', {'id': diplome.id}) }}">edit</a>

    {{ include('diplome/_delete_form.html.twig') }}
{% endblock %}
