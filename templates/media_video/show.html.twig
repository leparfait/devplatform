{% extends 'base.html.twig' %}

{% block title %}MediaVideo{% endblock %}

{% block body %}
    <h1>MediaVideo</h1>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ media_video.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ media_video.type }}</td>
            </tr>
            <tr>
                <th>Identif</th>
                <td>{{ media_video.identif }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ media_video.date ? media_video.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table> #}
    <div class="text-center">
            <a href="{{ path('media_video_index') }}" class="btn btn-outline-info">back to list</a>
            <a href="{{ path('media_video_edit', {'id': media_video.id}) }}" class="btn btn-outline-warning">edit</a>
    </div>
    <div style='height :50% ;width :50%' class="center-block embed-responsive embed-responsive-16by9 mt-5 mb-5">
        {{ media_video.video()|raw }}
     </div>
     <div class="text-center">
             {{ include('media_video/_delete_form.html.twig') }}
     </div>


{% endblock %}
